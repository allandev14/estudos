<?php

// Estudos de super globais 02

/*
variável  $_POST Um array associativo de variáveis passados para o script atual 
via método HTTP POST quando utilizado application/x-www-form-urlencoded 
ou multipart/form-data como valor do cabeçalho HTTP Content-Type na requisição.
*/

// Variável $_GET Um array associativo de variáveis passadas para o script atual via o método HTTP GET.